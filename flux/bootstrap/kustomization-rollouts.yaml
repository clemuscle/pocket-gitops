apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: pocket-rollouts
  namespace: flux-system
spec:
  interval: 1m
  path: ./rollouts           # ← dossier contenant helmrepositories.yaml, *.hr.yaml
  prune: true
  sourceRef: {kind: GitRepository, name: pocket-gitops}
  wait: true
  timeout: 3m
